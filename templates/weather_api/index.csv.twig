city,country,date,celsius,fahrenheit
{% for measurement in measurements %}
{{ city }},{{ country }},{{ measurement.date | date('Y-m-d') }},{{ measurement.celcius }},{{ measurement.fahrenheit }}
{% endfor %}