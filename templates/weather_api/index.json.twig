{
    "city": "Szczecin",
    "country": "PL",
    "measurements": [
        {% for measurement in measurements %}
        {
            "date": "{{ measurement.date | date('Y-m-d') }}",
            "celcius": "{{ measurement.celcius }}",
            "fahrenheit": "{{ measurement.fahrenheit }}"
        }{% if not loop.last %},{% endif %}
        {% endfor %}
    ]
}